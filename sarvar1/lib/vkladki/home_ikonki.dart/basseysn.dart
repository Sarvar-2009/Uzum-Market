import 'package:flutter/material.dart';

class Bassesyn extends StatelessWidget {
  const Bassesyn({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(backgroundColor: Colors.blue,),);
  }
}