import 'package:flutter/material.dart';

class Skidka extends StatelessWidget {
  const Skidka({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(backgroundColor: Colors.blue,),);
  }
}