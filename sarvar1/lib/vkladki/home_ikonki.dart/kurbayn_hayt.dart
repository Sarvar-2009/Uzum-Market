import 'package:flutter/material.dart';

class KurbaynHait extends StatelessWidget {
  const KurbaynHait({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(backgroundColor: Colors.blue,),);
  }
}