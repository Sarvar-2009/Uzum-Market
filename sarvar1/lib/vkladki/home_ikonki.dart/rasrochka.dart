import 'package:flutter/material.dart';

class Rasrochka extends StatelessWidget {
  const Rasrochka({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(backgroundColor: Colors.blue,),);
  }
}