import 'package:flutter/material.dart';

class Dostavkaedi extends StatelessWidget {
  const Dostavkaedi({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(backgroundColor: Colors.blue,),);
  }
}